<Window x:Class="EasySave.Views.SelectProcess"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select a running process"
        MinHeight="450" MinWidth="500"
        Height="500" Width="550"
        WindowStartupLocation="CenterOwner"
>
    <StackPanel Margin="10">
        <TextBlock Text="Select a running process:" FontWeight="Bold" Margin="0,0,0,8"/>
        <!-- Search bar -->
        <TextBox x:Name="SearchBox"
                 Height="28"
                 Margin="0,0,0,8"
                 VerticalContentAlignment="Center"
                 TextChanged="SearchBox_TextChanged"/>
        <ListView x:Name="ProcessListView" SelectionMode="Single" Height="320">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="180"/>
                    <GridViewColumn Header="PID" DisplayMemberBinding="{Binding Id}" Width="60"/>
                    <GridViewColumn Header="Memory (MB)" DisplayMemberBinding="{Binding MemoryMB}" Width="100"/>
                    <GridViewColumn Header="Path" DisplayMemberBinding="{Binding Path}" Width="120"/>
                </GridView>
            </ListView.View>
        </ListView>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="OkButton" Content="Add" Width="80" Height="30" Margin="0,0,10,0" IsEnabled="False" Click="OkButton_Click"/>
            <Button Content="Cancel" Width="80" Height="30" Click="CancelButton_Click"/>
        </StackPanel>
    </StackPanel>
</Window>
